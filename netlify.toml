# Next.js app lives in web/ — Netlify must build & publish from there.
# https://docs.netlify.com/build/configure-builds/monorepos/

[build]
  base = "web"
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
